<% if show_pagination? and @response.total_pages > 1 and not full_text_search? %>
 <div class="row record-padding">
  <div class="col-md-12">
    <%= render Blacklight::Response::PaginationComponent.new(response: @response) %>
  </div>
 </div>
<% end %>
